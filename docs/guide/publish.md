# 📦 发布插件

本文介绍如何打包和分享你的 MaiBot 插件。

## 打包插件

使用 CLI 工具将插件打包为 `.zip` 文件：

```bash
python -m mai_plugin_cli pack ./my_plugin
```

输出：
```
📦 正在打包：my_plugin
✅ 打包完成：my_plugin-v1.0.0.zip
```

打包时会自动：
- 排除 `__pycache__/`、`.pyc`、`.git/` 等无用文件
- 验证 `_manifest.json` 格式
- 生成标准命名格式：`{name}-v{version}.zip`

## 安装插件

将插件目录（或解压 .zip 后）放入 MaiBot 的 `plugins/` 目录：

```
MaiBot/
└── plugins/
    ├── __init__.py
    ├── emoji_manage_plugin/
    └── my_plugin/          ← 放这里
        ├── _manifest.json
        └── plugin.py
```

然后重启 MaiBot，插件会自动加载。

## 验证插件

发布前建议先验证：

```bash
python -m mai_plugin_cli validate ./my_plugin
```

## _manifest.json 版本管理

每次发布新版本时，更新 `version` 字段：

```json
{
  "manifest_version": 1,
  "name": "我的插件",
  "version": "1.1.0",
  "description": "插件描述",
  "author": "作者名",
  "dependencies": {}
}
```

遵循语义化版本（SemVer）：
- `1.0.0` → `1.0.1`：修复 Bug
- `1.0.0` → `1.1.0`：新增功能（向后兼容）
- `1.0.0` → `2.0.0`：破坏性变更

## 分享插件

1. 将 `.zip` 文件上传到 GitHub Release
2. 或者直接把插件目录推送到 GitHub 仓库
3. 在 MaiBot 社区分享你的插件链接

## 下一步

- 🏗️ 了解 [插件架构](/guide/architecture)
- 📤 查看 [发送 API](/api/send_api)
