# 测试打招呼插件

一个测试MaiScript功能的插件

**作者**：测试者  
**版本**：1.0.0  
**生成方式**：MaiScript 自动编译

## 命令列表

- `/hello` — 响应 /hello 命令
- `/roll` — 响应 /roll 命令
- `/time` — 响应 /time 命令

## 自主行为（Action）

- **安慰用户**：当有人表达悲伤或失落时、当有人说自己很难过时

## 安装

将 `test_hello/` 目录复制到 MaiBot 的 `plugins/` 目录，重启 MaiBot 即可。