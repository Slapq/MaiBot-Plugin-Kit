plugin:
  name: "测试打招呼插件"
  version: "1.0.0"
  author: "测试者"
  description: "一个测试MaiScript功能的插件"

commands:
  - name: "打招呼"
    match: "/hello"
    reply: "你好！{user_name}！😊"

  - name: "掷骰子"
    match: "/roll"
    python: |
      import random
      n = random.randint(1, 6)
      reply = f"🎲 你掷出了 {n} 点！"

  - name: "查时间"
    match: "/time"
    python: |
      import datetime
      now = datetime.datetime.now()
      reply = f"现在是 {now.strftime('%H:%M:%S')}"

actions:
  - name: "安慰用户"
    when:
      - "当有人表达悲伤或失落时"
      - "当有人说自己很难过时"
    reply: "没关系的！加油！💪"
